import { chromium } from '@playwright/test';
const browser=await chromium.launch({executablePath:'C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe',headless:true});const page=await browser.newPage({viewport:{width:1440,height:1000}});await page.goto('http://127.0.0.1:5173/');await page.locator('.learning-films').scrollIntoViewIfNeeded();await page.getByRole('button',{name:'Play films'}).click();await page.waitForTimeout(6000);console.log(await page.locator('video').evaluateAll(v=>v.map(x=>({src:x.currentSrc,error:x.error?.code,ready:x.readyState,time:x.currentTime,paused:x.paused}))));await page.screenshot({path:'work/qa/films.png'});await browser.close();
